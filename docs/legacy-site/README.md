# Legacy site (not served)

GitHub Pages serves the **`gh-pages` branch**, not these files. This directory
holds the old root `index.html` and `blog/` sources that used to shadow it —
moved here in v2 after an update went to the wrong file. Edit the site on
`gh-pages`.
